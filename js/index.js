/* ---------------------- */
/* NAVIGATION             */
/* ---------------------- */
document.addEventListener('DOMContentLoaded', () => {
  const startBtn = document.getElementById('start');
  startBtn?.addEventListener('click', () => location.href = 'capture.html');
});
